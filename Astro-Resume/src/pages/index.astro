---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Test Home — Netlify Identity</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
</head>
<body style="font-family: system-ui, sans-serif; padding: 2rem;">
  <h1>Test Homepage</h1>
  <p>This page includes the Netlify Identity widget.</p>

  <div data-netlify-identity-button
       style="display:inline-block;padding:0.5rem 1rem;border:1px solid #333;border-radius:4px;cursor:pointer;">
    Login / Signup
  </div>

  <!-- Load Netlify Identity widget as *inline* so Astro doesn't mess with it -->
  <script is:inline src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

  <!-- Init + redirect after login -->
  <script is:inline>
    if (window.netlifyIdentity) {
      window.netlifyIdentity.init();

      // Optional: log in / sign up from button
      // (data-netlify-identity-button already works automatically,
      // but we can also redirect after login)
      window.netlifyIdentity.on("login", () => {
        document.location.href = "/admin/";
      });
    }
  </script>
</body>
</html>
